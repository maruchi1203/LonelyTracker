# 1일차 진행사항 (2026-08-18)

기준 문서: `자기개발/프로젝트-AI일정관리앱-기획.md`
관련 문서: [일정 CRUD API 설계](./일정-CRUD-API-설계.md)

## 요약

기획서 로드맵의 **1일차(프로젝트 뼈대) + 2~3일차(CRUD·DB 연결) 범위를 하루에 완료**했다.
일정 등록 → 조회 → 완료 체크 → 삭제가 브라우저에서 실제로 동작하는 상태.

로드맵 대비 진행:

| 로드맵 | 계획 | 상태 |
|---|---|---|
| 1일차 | 프로젝트 뼈대, CRUD API 설계 | ✅ 완료 |
| 2-3일차 | CRUD 완성 + DB 연결 + 배포 세팅 | ✅ CRUD·DB 완료 / ⬜ 배포 미착수 |
| 4-5일차 | AI 자연어 파싱 | ⬜ |
| 6일차 | AI 코칭/추천 | ⬜ |
| 7일차 | 주간 요약 | ⬜ |

---

## 1. 개발 환경 구축

시작 시점에 개발 도구가 사실상 없는 상태여서 환경 구축부터 진행했다.

| 항목 | 조치 |
|---|---|
| JDK | 없음 → Temurin JDK 21.0.12 LTS 설치, `JAVA_HOME`·PATH 등록 |
| Gradle | 없음 → 프로젝트 wrapper(9.5.1)가 자동 처리하므로 별도 설치 불필요 |
| PostgreSQL | `data` 폴더만 남은 고아 상태 → 백업 후 17.11 재설치 |
| Node / npm | v24.18.0 / 11.16.0 (기존 설치분 사용) |
| DBeaver | DB·롤 생성 및 데이터 확인용으로 설치 |

**PostgreSQL 특이사항** — `C:\Program Files\PostgreSQL\17\`에 `data`와 `pgAdmin 4`만 있고 `bin`이 없었다.
로그상 2026-02-19에 정상 종료된 뒤 서버만 제거된 상태. 기존 `data`는 삭제하지 않고
`data_backup_20260218`로 이름만 바꿔 보존한 뒤 새로 설치했다.

**DB 구성**

| 항목 | 값 |
|---|---|
| 데이터베이스 | `lonelytracker` |
| 롤 | `lonelytracker` (LOGIN) |
| 소유자 | `lonelytracker` |
| 인코딩 | UTF8 |

소유자 지정이 중요하다. PostgreSQL 15부터 `public` 스키마에 아무나 테이블을 만들지 못하므로,
DB 소유자를 앱 롤로 지정해야 Hibernate의 `ddl-auto: update`가 테이블을 자동 생성할 수 있다.

---

## 2. 프로젝트 구조

```
LonelyTracker/
├─ .claude/launch.json     개발 서버 실행 설정
├─ .gitignore              .env, build/, node_modules/ 제외
├─ backend/                Spring Boot 4.1.0 · Java 21 · Gradle Kotlin DSL
│  ├─ .env                 실제 접속정보 (git 제외)
│  ├─ .env.example         템플릿
│  ├─ gradle.properties    한글 경로 대응 인코딩 설정
│  └─ src/main/
│     ├─ java/com/lonelytracker/backend/
│     │  ├─ BackendApplication.java
│     │  ├─ common/        예외 처리, JPA 감사 설정
│     │  └─ schedule/      도메인 (엔티티~컨트롤러)
│     └─ resources/application.yml
├─ frontend/               React 19 · TypeScript · Vite 8
│  └─ src/
│     ├─ api/schedules.ts  fetch 래퍼
│     ├─ types/schedule.ts 백엔드 DTO와 짝 맞춘 타입
│     └─ components/       ScheduleForm, ScheduleList
└─ docs/
```

**의존성**: web(webmvc), data-jpa, postgresql, validation, lombok, devtools, actuator

---

## 3. 백엔드 구현

### 도메인

`Schedule` 엔티티 — 제목, 설명, 시작/종료, 종일여부, 상태, 분류, 생성경로, 생성/수정시각.

`status`(PLANNED/DONE/SKIPPED)와 `source`(MANUAL/AI_PARSED)는 당장 쓰지 않지만 1일차부터 넣었다.
각각 7일차 주간 수행률과 4~5일차 AI 파싱 구분에 필요한데, 나중에 추가하면 스키마 변경이 발생한다.

### 엔드포인트

| 메서드 | 경로 | 응답 |
|---|---|---|
| GET | `/api/schedules?from=&to=&status=` | 200 |
| GET | `/api/schedules/{id}` | 200 / 404 |
| POST | `/api/schedules` | 201 + Location |
| PUT | `/api/schedules/{id}` | 200 / 404 |
| PATCH | `/api/schedules/{id}/status` | 200 / 404 |
| DELETE | `/api/schedules/{id}` | 204 / 404 |

### 설계상 지킨 것

- **엔티티를 직접 반환하지 않는다.** `ScheduleResponse`로 변환한다. `open-in-view: false` 환경에서
  지연 로딩이 직렬화 중 터지는 것을 막고, 엔티티 변경이 API 계약에 새지 않게 한다.
- **setter 대신 의도가 드러나는 메서드.** `update()`, `changeStatus()`만 열어둔다.
- **예외는 `@RestControllerAdvice`에서 일괄 변환.** 컨트롤러에 try-catch가 없다.

---

## 4. 프론트엔드 구현

일정 목록 · 추가 폼 · 완료 체크박스 · 삭제 버튼으로 구성된 단일 화면.

- `/api` 요청은 Vite 프록시가 8080으로 넘긴다. 호스트를 하드코딩하지 않았으므로
  나중에 프론트/백엔드를 합쳐 배포해도 코드 수정이 필요 없다.
- 백엔드 DTO와 동일한 TypeScript 타입을 선언해 필드명 오타를 컴파일 단계에서 잡는다.
- 에러는 백엔드가 내려준 `message`를 그대로 화면에 표시한다.

---

## 5. 검증 결과

**API** — 6개 엔드포인트 전부 정상 동작 확인.

**에러 처리** — 아래 모두 통일된 JSON으로 응답:

| 상황 | 코드 |
|---|---|
| title 누락 | 400 |
| endAt < startAt | 400 |
| 깨진 JSON | 400 |
| 잘못된 enum 값 | 400 |
| 없는 id | 404 |

**브라우저** — 일정 등록 → 목록 자동 정렬(09:30이 15:30 앞) → 카운터 갱신 →
완료 토글 → 삭제까지 동작 확인. 콘솔 에러 없음.

**빌드** — 백엔드 `gradlew build` (테스트 포함) 성공, 프론트 `npm run build` (타입 검사 포함) 성공.

---

## 6. 구현 중 해결한 문제

오늘 가장 시간이 든 부분이자 재발 가능성이 있는 항목들.

### ① 한글 경로 때문에 테스트가 실행조차 안 됨

프로젝트 경로에 `바탕 화면`이 포함돼, Gradle이 UTF-8로 쓴 테스트 워커 `@argfile`을
Java 런처가 시스템 인코딩(CP949)으로 읽어 클래스패스가 깨졌다.
증상은 `ClassNotFoundException`인데 클래스 파일은 멀쩡히 존재하는 형태.

→ `gradle.properties`에 `org.gradle.jvmargs=-Dfile.encoding=MS949`로 해결.

**근본 대책은 프로젝트를 ASCII 경로로 옮기는 것.** OneDrive가 `node_modules`
수만 개 파일을 동기화하며 빌드 중 파일 잠금을 일으키는 문제도 함께 사라진다.

### ② Spring Boot는 `.env`를 자동으로 읽지 않는다

`.env`는 Node/Docker 관례라 `${DB_PASSWORD}`는 OS 환경변수만 찾는다.

→ `spring.config.import: optional:file:.env[.properties]` 로 해결.
`optional:`을 붙였으므로 `.env`가 없는 배포 환경에서도 앱은 정상 기동한다.

### ③ `:param is null` 패턴이 PostgreSQL에서 실패

기간·상태 조회를 JPQL `(:from is null or s.startAt >= :from)`로 짰더니
`$1 매개 변수의 자료형을 알수가 없습니다` 오류. PostgreSQL은 null 비교에만 쓰인
파라미터의 타입을 추론하지 못한다.

→ **Specification(동적 쿼리)** 으로 교체. 조건이 있을 때만 predicate를 붙이므로
생성되는 SQL도 더 깔끔하다.

### ④ 원시 `boolean`은 JSON 선택 필드로 쓸 수 없다

`allDay`를 생략하고 POST하면 `Cannot map null into type boolean`.

→ 요청 DTO에서 `Boolean` 래퍼를 쓰고 서비스에서 null을 false로 취급.

### ⑤ `updatedAt`이 갱신 전 값으로 응답됨

`@LastModifiedDate`는 flush 시점에 채워진다. 변경 감지만 믿고 바로 DTO를 만들면
응답에는 수정 전 `updatedAt`이 실린다.

→ 수정·상태변경 시 `saveAndFlush` 후 응답 생성.

### ⑥ 스택트레이스가 API 응답에 노출됨

에러 응답에 자바 스택트레이스가 통째로 실려 나왔다. devtools가
`include-stacktrace=always`를 기본으로 심기 때문.

→ `spring.devtools.add-properties: false`로 차단.

추가로 **Boot 4에서 `server.error.include-stacktrace`는 error 레벨로 폐기**되고
`spring.web.error.include-stacktrace`로 옮겨졌다. 3.x 기준 자료를 보고 옛 이름을 쓰면
조용히 무시된다.

### ⑦ YAML Map 키 이스케이프

`logging.level`과 `spring.jpa.properties`는 Map이라, 점이 든 키는 YAML 계층으로
오해될 수 있다. IDE도 `YAML_SHOULD_ESCAPE` 경고를 띄운다.

→ `"[org.hibernate.SQL]"`, `"[hibernate.format_sql]"` 처럼 `[]`로 감싼다.

이 과정에서 `hibernate.dialect` 명시가 불필요하다는 것도 확인했다
(`HHH90000025` 경고). Hibernate가 JDBC 연결을 보고 자동 선택하므로 제거했다.

---

## 7. 커밋

| 커밋 | 내용 |
|---|---|
| `90b535a` | 프로젝트 스캐폴딩 (SpringBoot + React) |
| `4f4da1f` | 일정 CRUD 백엔드·프론트 구현 |

---

## 8. 추가 진행 — 스키마 관리와 카테고리 승격

1일차 후반에 이어서 진행한 내용. 요구사항 정리에서 출발했다.

| 요구 | 처리 |
|---|---|
| description을 마크다운으로 | `TEXT` 컬럼, 원문 저장·화면 렌더링 |
| 화면에서 카테고리 필터 | `GET /api/schedules?category=` + 사이드바 |
| ScheduleSource 삭제 | 제거, 성격 구분은 category가 담당 |
| 카테고리 계층 (`\` 구분) | Category 엔티티 + `parent_id` + `path` |
| UI는 TickTick 방향 | 사이드바 기반 구조로 선회 (외형은 이후 작업) |
| JPA 준비 | Flyway 도입, `ddl-auto`는 `validate`로 |
| 컨트롤러 주석 | 6개 메서드에 역할·응답코드 주석 |

### 8-1. Flyway 도입

`ddl-auto: update`가 컬럼 삭제와 타입 변경을 하지 않아 손으로 SQL을 돌려야 했던 문제를
정면으로 해결했다. 스키마를 고치는 주체를 Flyway 하나로 통일하고 `ddl-auto`는 `validate`로 내렸다.

| 파일 | 내용 |
|---|---|
| `V1__init.sql` | 기존 스키마 베이스라인. 기존 DB에서는 기록만 되고 실행되지 않는다 |
| `V2__drop_source_and_widen_columns.sql` | source 제거, description → TEXT, category 확장 |
| `V3__add_category_table.sql` | category 테이블 생성 + 기존 문자열 이관 + FK 연결 |

실행 로그가 설계 그대로 나왔다.

```
Successfully baselined schema with version: 1
Migrating schema "public" to version "2 - drop source and widen columns"
Migrating schema "public" to version "3 - add category table"
```

**규칙: 이미 적용된 마이그레이션 파일은 수정 금지.** checksum이 어긋나면 앱이 기동을 거부한다.
변경이 필요하면 새 버전을 추가한다.

### 8-2. Category 엔티티 승격

문자열 컬럼에서 엔티티로 올렸다. 판단 근거는 TickTick식 사이드바가
순서·색상·접힘·보관 상태를 요구하는데 문자열에는 그런 정보를 둘 곳이 없다는 점,
그리고 이름 변경 시 관련 행을 전부 UPDATE해야 한다는 점이었다.

계층은 `parent_id`로 잡되 전체 경로 `path`를 함께 보관한다.
**중복 보관의 대가로 "하위 포함 필터"가 재귀 조회 없이 prefix 비교 한 번으로 끝난다.**
대신 이름이 바뀌면 후손의 `path`도 함께 갱신할 책임이 생긴다.

기존 데이터 이관은 마이그레이션 안에서 처리했다. `능력\개발\SpringBoot` 문자열 하나에서
상위 경로 3개를 모두 생성해야 계층이 완성되므로, `generate_series`로 경로 접두사를 펼쳐 넣었다.

이관 결과 7개 카테고리가 만들어지고 부모 연결까지 정확히 잡혔다.

```
능력
  개발
    React
    SpringBoot
  커리어
취미
  게임개발
```

**API**

| 메서드 | 경로 | 설명 |
|---|---|---|
| `GET` | `/api/categories` | 전체 목록 (경로순) |
| `POST` | `/api/categories` | 생성. 중간 단계가 없으면 함께 생성 |
| `PATCH` | `/api/categories/{id}/name` | 이름 변경. 후손 경로 자동 갱신 |
| `PATCH` | `/api/categories/{id}/appearance` | 색상·순서·접힘·보관 |
| `DELETE` | `/api/categories/{id}` | 하위 있으면 400, 일정은 미분류로 남김 |

일정 저장 시에는 `categoryPath` 문자열을 받아 **없으면 중간 단계까지 만들어 붙인다**(get-or-create).
분류를 자유 입력으로 쓰기 위한 선택이다.

### 8-3. 검증

- 이름 변경: `능력\개발` → `능력\학습` 한 번에 후손 2개 경로가 갱신되고,
  일정도 그대로 따라왔다. **FK 참조라 일정 테이블은 건드릴 필요가 없다** — 승격의 실익이 확인된 지점.
- 하위 있는 카테고리 삭제: 400과 안내 메시지
- 하위 포함 필터: `능력` 4건, `능력\개발` 2건, `취미` 1건, `능력강화` 0건
- 화면에서 `능력\개발\DB\Flyway` 입력 → `DB`, `Flyway` 두 단계가 자동 생성되어 사이드바에 반영
- 마크다운 원문 왕복 일치, LIKE 와일드카드(`%`) 주입 시 리터럴 처리

### 8-4. 이 과정에서 걸린 문제

**Boot 4는 라이브러리만으로 자동설정이 붙지 않는다.**
`flyway-core`와 `flyway-database-postgresql`을 넣었는데 **오류도 없이 Flyway가 아예 실행되지 않았다.**
로그에 한 줄도 안 나왔다.

원인은 Boot 4의 오토컨피그 모듈 분리였다. 연결 코드가 `spring-boot-flyway`라는 별도 모듈에 있었다.
그 jar 안에는 이런 파일이 들어 있다.

```
META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports
  └─ org.springframework.boot.flyway.autoconfigure.FlywayAutoConfiguration
```

이 등록부가 있어야 `spring.flyway.*` 설정을 읽는 코드가 동작한다.
**설정은 처음부터 맞았고, 그 설정을 해석할 어댑터 계층이 없었던 것이다.**
Spring Boot는 모르는 설정 키에 오류를 내지 않으므로 조용히 무시됐다.

같은 패턴을 오늘만 세 번 만났다.

| 3.x 기준 자료 | Boot 4 실제 |
|---|---|
| `spring-boot-starter-web` | `spring-boot-starter-webmvc` |
| `spring-boot-starter-test` | `-webmvc-test`, `-data-jpa-test` 등으로 분리 |
| `flyway-core` | `spring-boot-flyway` + `flyway-database-postgresql` |

**대응법**: "라이브러리는 넣었는데 반응이 없다" → BOM에서 대응 `spring-boot-*` 모듈을 찾는다.
BOM은 `~/.gradle/caches/modules-2/files-2.1/org.springframework.boot/spring-boot-dependencies/`
아래 POM 파일이며, `spring-boot-*` 모듈이 312개 정의돼 있다.
공식 문서의 Managed Dependency Coordinates 페이지에는 **서드파티 버전만** 있고
Spring Boot 자체 모듈은 없으므로 POM을 직접 봐야 한다.

진단에는 `bootRun --args='--debug'`의 자동설정 리포트가 유용하다.
`Positive matches` / `Negative matches`로 어떤 오토컨피그가 왜 적용되지 않았는지 나온다.

---

## 9. 다음에 할 일

**우선순위 높음**

- **화면 구성** — TickTick 방향으로. 사이드바 접기/색상 지정, 날짜별 그룹핑,
  오늘/이번 주 필터, 수정 화면(PUT API는 있으나 진입점 없음)
- **기능 On/Off 토글** — 단일 사용자로 결정했으므로 `localStorage` 기반
- 마크다운 렌더링 (`react-markdown`, XSS 방어 포함)
- 배포 세팅

**이후**

- 4~5일차: `POST /api/schedules/parse` (자연어 → 일정 초안)
- 6일차: `GET /api/coaching/daily`
- 7일차: `GET /api/reports/weekly`

**정리 대상**

- 테스트 코드 — 현재 `contextLoads` 하나뿐. 카테고리 이름 변경처럼
  후손까지 파급되는 로직은 테스트가 있어야 안전하다
- 프로젝트를 ASCII 경로로 이전
- 카테고리 위치 이동(부모 변경) 기능 — 현재는 이름 변경만 가능

**확정된 결정 (2026-08-18)**

| 항목 | 결정 |
|---|---|
| 사용자 범위 | 단일 사용자. `userId` 없음 |
| 카테고리 | Category 엔티티. 계층은 `parent_id` + `path` |
| 스키마 관리 | Flyway. `ddl-auto`는 `validate` |
| 계층 구분자 | `\` 유지 |
