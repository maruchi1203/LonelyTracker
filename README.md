# LonelyTracker

> 자기개발 기록을 위한 일정 관리 웹앱

## 만든 이유

1. 스케줄 앱 **TickTick**을 사용하며 예정된 일과 할 일을 일일히 정리하는 게 귀찮았다.
2. Claude나 GPT 같은 AI를 사용할 일이 많은데 일정도 AI가 정리해주면 좋겠다 싶었다.
3. **ReclaimAI** 같은 서비스 이용해도 되긴 하는데, 난 AI가 평일 기록들을 보고 주말에 뭐할지, 시간날 때 뭐 공부하면 좋을지 일정 추천도 해주면 좋겠다 싶었다.

---

## 현재 상태

Claude 코드를 사용해 **일정 등록 → 분류 → 완료 체크 → 조회**가 브라우저에서 동작하게 구성된 상태이다.

| 기능                            | 상태         |
| ------------------------------- | ------------ |
| 일정 CRUD (기간·상태·분류 필터) | ✅           |
| 사용자별 카테고리 목록          | ✅           |
| 소유자 기반 데이터 격리         | ✅           |
| 자연어 입력 파싱 (AI)           | ⬜ 다음 단계 |
| OAuth 로그인                    | ⬜ 다음 단계 |
| 배포                            | ⬜ 다음 단계 |

---

## 기술 스택

| 계층        | 기술                         | 선택 이유                                                   |
| ----------- | ---------------------------- | ----------------------------------------------------------- |
| 백엔드      | Spring Boot 4.1 / Java 21    | 실무에서 쓰는 스택으로 익히려는 목적                        |
| DB          | PostgreSQL 17                | 재귀 쿼리·`generate_series` 등 마이그레이션에서 실제로 활용 |
| 스키마 관리 | Flyway                       | `ddl-auto`로는 컬럼 삭제·데이터 이관이 불가능 (아래 참고)   |
| 프론트      | React 19 / TypeScript / Vite | 백엔드 DTO와 타입을 맞춰 필드명 오타를 컴파일 단계에서 차단 |
| 테스트      | JUnit 5 + Testcontainers     | 실제 PostgreSQL 컨테이너로 검증 중                          |

---

## 설계에서 신경 쓴 것

### 권한 없는 접근은 403이 아니라 404

```java
scheduleRepository.findById(id)
        .filter(schedule -> ownerIdOf(schedule).equals(userId))
        .orElseThrow(() -> new NotFoundException(...));
```

403은 "그 id는 존재하지만 네 것이 아니다"를 알려준다. 남의 데이터 존재 여부가 새므로 404로 통일했다.

### 하드코딩을 처음부터 최대한 배제한다.

추후에 값을 쉽게 바꾸기 위해서는 하드코딩을 제거하는 게 좋겠다고 판단했다.

---

## 겪은 문제와 해결

전체 기록은 [1일차 진행사항](docs/1일차-진행사항.md)에 있다.

### `ddl-auto: update`는 컬럼을 지우지 않는다

엔티티에서 필드를 삭제했는데 DB 컬럼이 `NOT NULL`로 남아 **모든 INSERT가 실패**했다. `update`는 이름 그대로 추가만 한다.
→ Flyway 도입. `ddl-auto`는 `validate`로 내려 스키마를 고치는 주체를 하나로 통일했다.

### 라이브러리는 넣었는데 아무 일도 일어나지 않았다

`flyway-core`를 추가하지 않아 어댑터가 부재하는 문제가 생겼었다.
→ jar 안의 `AutoConfiguration.imports` 등록부를 직접 열어 확인하고 해결.

### JVM 크래시 11건 — 우회 설정이 힙 상한을 지웠다

프로젝트 경로에 한글(바탕화면)이 있어 `org.gradle.jvmargs` 로 인코딩을 지정했는데, 이 설정이 Gradle 기본 JVM 옵션을 대체한다는 걸 몰랐다.

힙 상한이 사라져 JVM 기본값(물리 메모리의 1/4 = 8.5GB)이 적용됐고,
빌드마다 새로 뜨는 JVM이 그만큼씩 예약하며 메모리가 고갈됐다. 그 때문에 컴퓨터가 터졌었다.
→ 힙 상한 명시. 근본 원인인 한글 경로를 제거하니 우회 설정 자체가 필요 없어졌다.

---

## 문서

| 문서                                     | 내용                                                   |
| ---------------------------------------- | ------------------------------------------------------ |
| [1일차 진행사항](docs/1일차-진행사항.md) | 환경 구축부터 구현까지. **겪은 문제 11건**과 학습 기록 |

---

## 다음 단계

의도적으로 남겨둔 것들이다. 기능을 넓히기보다 이미 만든 것의 완성도를 먼저 봤다.

- **자연어 입력 파싱** — `"내일 3시 회의"` → 일정 자동 등록. 이 앱의 원래 목표
- **OAuth 로그인** — 소유 구조는 갖췄고 인증 판별만 남았다
- **배포** — 프론트 빌드를 백엔드 `static/` 에 넣어 JAR 하나로
- **화면 개선** — 날짜별 그룹핑, 마크다운 렌더링, 카테고리 관리 UI
