# org.gradle.jvmargs 는 Gradle 기본 JVM 옵션을 "덮어쓴다".
# 힙 상한을 함께 적지 않으면 JVM 기본값(물리 메모리의 1/4 = 이 PC에서 8.5GB)이 적용되고,
# 빌드마다 포크되는 JVM이 그만큼씩 예약하면서 메모리가 고갈된다.
# 실제로 2026-08-19 에 이 원인으로 JVM 크래시(hs_err) 11건이 발생했다.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
