# 이 파일을 .env 로 복사한 뒤 실제 값을 채우세요. .env 는 git에 올라가지 않습니다.

# PostgreSQL 접속 정보
DB_URL=jdbc:postgresql://localhost:5432/lonelytracker
DB_USERNAME=lonelytracker
DB_PASSWORD=

# OpenAI API 키 (4-5일차 자연어 파싱 단계에서 사용)
OPENAI_API_KEY=
